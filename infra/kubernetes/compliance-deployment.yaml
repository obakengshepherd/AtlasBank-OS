apiVersion: apps/v1
kind: Deployment
metadata:
  name: compliance-api
  namespace: atlasbank
spec:
  replicas: 2
  selector:
    matchLabels:
      app: compliance-api
  template:
    metadata:
      labels:
        app: compliance-api
    spec:
      containers:
        - name: compliance-api
          image: atlasbank/compliance-api:latest
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: atlasbank-config
          env:
            - name: ConnectionStrings__DefaultConnection
              valueFrom:
                secretKeyRef:
                  name: db-secrets
                  key: compliance-connection-string
---
apiVersion: v1
kind: Service
metadata:
  name: compliance-api-service
  namespace: atlasbank
spec:
  selector:
    app: compliance-api
  ports:
    - port: 80
      targetPort: 8080
